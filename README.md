# shopping_spree
Submission for Sleek ShoppingSpree challenge


```
λ python solver.py -h
usage: solver.py [-h] -f FILE -o OUTPUT

Solves ShoppingSpree problem with a modified version of dijkstra's algorithm.

optional arguments:
  -h, --help            show this help message and exit
  -f FILE, --file FILE  The file containing the mall.
  -o OUTPUT, --output OUTPUT
                        The output file.
```
